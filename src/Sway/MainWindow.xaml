<Window x:Class="Sway.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sway"
        mc:Ignorable="d"
        Title="Sway"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize" Width="300">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,4">
            <Label Content="Move the mouse after seconds:" VerticalAlignment="Center" Padding="0" Width="200"/>
            <TextBox Text="{Binding RunAfterSeconds, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center" Width="56" Height="23"
                     ToolTip="Valid range: 2 to 1000 seconds"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,6">
            <Label Content="Mouse sway length:" VerticalAlignment="Center" Padding="0" Width="200"/>
            <TextBox Text="{Binding SwayLength, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center" Width="56" Height="23"
                     ToolTip="Valid range: 0 to 50 pixels"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,6">
            <CheckBox Content="Auto Start" IsChecked="{Binding AutoStart}" Margin="0,2"/>
            <CheckBox Content="Run when windows locked" IsChecked="{Binding RunWhenWindowsLocked}" Margin="0,4"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10">
            <Button Content="{Binding RunningStatus}" Command="{Binding StartStopCommand}"
                    Width="100" Height="32" Margin="0,0,8,0"/>
            <Button Content="Exit" Click="Button_Click"
                    Width="100" Height="32"/>
        </StackPanel>
    </Grid>
</Window>
